# Akura Phase 3 — Settings & billing skeleton

- [x] `setting_definitions` (~17), `tenant_settings`, `setting_history` + RLS
- [x] `settings.get` / `setSetting` / self-generating settings UI
- [x] `plans`, `subscriptions` (30-day Growth trial on signup), usage counters
- [x] `assertQuota` / `assertWritable` (attendance exempt by convention)
- [x] Admin billing usage panel
- [x] Tests in `tests/settings-quota.test.ts`

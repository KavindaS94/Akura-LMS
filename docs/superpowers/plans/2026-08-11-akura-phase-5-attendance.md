# Akura Phase 5 — Attendance

- [x] class_sessions, attendance, attendance_edits + RLS
- [x] Open today’s session; default Present; 3-tap marking
- [x] Teacher UI with localStorage draft + retry
- [x] Lock via `attendance.lock_hours`; admin edit with reason → attendance_edits
- [x] Reports: %, streak, eligibility, CSV + print/PDF
- [x] `attendance.marked` outbox; marking allowed in read_only
- [x] `tests/attendance-lock.test.ts`
